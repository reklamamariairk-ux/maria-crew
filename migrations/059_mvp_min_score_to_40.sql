-- Новый смысл mvp_min_score: порог «практического» балла (отзыв + чек-лист + план),
-- БЕЗ тайного покупателя. По решению пользователя — лучший назначается если этот
-- балл > 40, а не если общий computedScore > 80 (старая интерпретация).
UPDATE mvp_config SET mvp_min_score = 40 WHERE id = 1;
