services:
  - type: web
    name: maria-crew
    runtime: node
    # Тесты до build — если падают, деплой останавливается
    buildCommand: npm install --include=dev && npm test -- --ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
